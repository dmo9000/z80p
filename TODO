* only execute x cycles per second 
* clean up leak in terminal scroll code
* various missing ANSI code support in libansicanvas
* cursor
* batch screen updates (ie. don't update display after every character is output)
* hardware clock interrupt
* build on Windows

